// ==========================================
// GMAX STUDIO MANAGEMENT SYSTEM - PRISMA SCHEMA
// Multi-Studio, Multi-Location Architecture
// ==========================================

generator client {
  provider = "prisma-client"
  output   = "../../lib/generated/prisma"
  runtime  = "workerd"
}

datasource db {
  provider = "postgresql"
}
